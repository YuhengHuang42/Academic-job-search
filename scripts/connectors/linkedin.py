from __future__ import annotations

from typing import Any


def search(query: dict[str, Any]) -> list[dict[str, Any]]:
    """Placeholder connector.

    LinkedIn generally requires an approved API or compliant data provider.
    This connector intentionally returns no rows until a compliant backend is configured.
    """
    _ = query
    return []
